import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx' // 위에서 작성한 코드가 들어있는 파일

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
